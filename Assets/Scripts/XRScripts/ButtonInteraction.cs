using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

public class ButtonInteraction : MonoBehaviour
{
    /*public DoorAnimatorController doorController;
    private XRGrabInteractable grabInteractable;8

   

    void Start()
    {
        grabInteractable = GetComponent<XRGrabInteractable>();
        grabInteractable.activated.AddListener(x =>OnSelectEntered());
    }

    private void OnSelectEntered(SelectEnterEventArgs args)
    {
        doorController.ToggleDoor1();
    } */
}